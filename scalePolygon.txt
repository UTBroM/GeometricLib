/** Author : Paul TREHIOU & Victor SENE
 *  Date : Decembre 2014
 **/

LEXICON
========
inpoly: the polygon wich we extend
factor: float wich is the coefficient of expension relative to a reference point (we take the head for the reference point)
i: integer
referencePoint : PointElement wich keep the initial head

ALGORITHM
=========
Data:   Polygon: inpoly
        Float: factor
Return: inpoly


function scalePolygon(inpoly : Polygon, factor : Float) : Polygon
Begin

	for i = 1 to size(inpoly) do

		head(inpoly) <- next(head(inpoly))

		x(value(head(inpoly)) = factor * (x(value(head(inpoly))
		y(value(head(inpoly)) = factor * (y(value(head(inpoly))

	done

	scalePolygon <- inpoly
End
